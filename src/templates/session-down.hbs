{{#if isReminder}}[Reminder] {{/if}}BGP Session Down: {{label}} on {{routerName}}
---
(This is an automated notification from OpenIX NOC)

The BGP session for {{label}} on {{routerName}} is currently not established.
{{#if isReminder}}
This is a periodic reminder. The session has been down since the last state change.
{{else}}
This notification was triggered upon detecting the session going down.
{{/if}}

Router:    {{routerName}}
Protocol:  {{protocolName}}
Peer:      {{label}}
Status:    {{status}}
Time:      {{timestamp}}

--
OpenIX NOC
noc@openix.ong
